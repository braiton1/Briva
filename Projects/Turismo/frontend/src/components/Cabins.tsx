import type { Cabin } from '../data/siteContent'
function Cabins({cabins}:{cabins:Cabin[]}){return <section className="cabins shell" id="cabanas"><div className="heading"><p className="eyebrow">Nuestros refugios</p><h2>Elegí cómo querés<br/>vivir la montaña.</h2></div><div className="cabins__grid">{cabins.map(cabin=><article className="cabin" key={cabin.id}><img src={cabin.image} alt={cabin.name}/><div className="cabin__body"><div className="cabin__meta"><span>{cabin.guests}</span><span>{cabin.size}</span></div><h3>{cabin.name}</h3><p>{cabin.description}</p><ul>{cabin.amenities.map(item=><li key={item}>{item}</li>)}</ul><div className="cabin__bottom"><strong>{cabin.price}</strong><a href="#reservar">Consultar →</a></div></div></article>)}</div><p className="demo-note">Tarifas ficticias de referencia. El valor final depende de fechas y temporada.</p></section>}
export default Cabins
